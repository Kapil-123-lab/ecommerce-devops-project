apiVersion: v1
kind: Service

metadata:
  name: ecommerce-service
  namespace: ecommerce

spec:
  selector:
    app: ecommerce-backend

  ports:
  - port: 80
    targetPort: 5000

  type: LoadBalancer
